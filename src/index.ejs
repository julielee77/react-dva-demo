<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>大数据平台 | 数据流通</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=0">
    <% if (htmlWebpackPlugin.options.production) { %>
        <% htmlWebpackPlugin.files.css.forEach(item => { %>
            <link href="${item}" rel="stylesheet">
        <% }) %>
    <% } %>
</head>
<body>

<div id="root"></div>

<% if (htmlWebpackPlugin.options.production) { %>
    <% htmlWebpackPlugin.files.js.forEach(item => { %>
        <script src="${item}" charset="utf-8"></script>
    <% }) %>
<% } %>
</body>
</html>
